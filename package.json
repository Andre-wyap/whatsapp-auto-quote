{
  "name": "whatsapp-auto-quote",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Send-only delivery layer that forwards packaged quote messages from n8n to a client over WhatsApp via Evolution API, plus a connect/monitor dashboard.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "@fastify/cookie": "^11.0.1",
    "fastify": "^5.1.0"
  }
}
